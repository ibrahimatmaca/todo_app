import 'package:flutter/material.dart';

final trailingEmptyIcon = Icon(
  Icons.circle,
  color: Colors.grey,
  size: 50,
);

final trailingDoneIcon = Icon(
  Icons.done,
  color: Colors.green,
  size: 50,
);

final addIcon = Icon(
  Icons.add,
  size: 35,
);
